const path = require('path');
const join = path.join;

function download(args) {
  const dest = path.join(process.cwd(), args.path);
  console.log(`dest`);
  console.log(dest);
}

module.exports = download;